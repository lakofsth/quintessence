QUINTESSENCE – PENDING CONSISTENCY FINDINGS (2; review when it suits – an ignored finding re-presents next session; [T2 stale?] pairs adjudicated as fine: `qq waveoff <memory> <head>` keeps them quiet until the HEAD moves again):
- [T1 link] 2 unresolved [[link]](s) (may be intentional to-write markers): foo, bar
- [T2 stale?] memory betamem (2026-01-01) vs HEAD beta (updated 2026-06-01T00:00:00Z, sim 0.71) – verify the memory's claims against the HEAD; edit or wave off (`qq waveoff betamem beta`).

QUINTESSENCE OPEN LOOPS – most-recently-worked first; `qq brief <topic>` to resume, `qq menu` for all:
  MAP        alpha                        alpha essence for surface freeze
  today      beta                         beta essence for surface freeze pairing
