# build/runtime droppings a checkout can actually produce. Store-side runtime files are
# covered by the .gitignore `qq init` writes into each store; state-dir files live outside
# git entirely (QQ_STATE_DIR).
*.log
__pycache__/
*.pyc
